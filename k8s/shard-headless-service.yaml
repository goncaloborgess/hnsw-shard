apiVersion: v1
kind: Service
metadata:
  name: vectordb-shard-headless
  namespace: vectordb
  labels:
    app: vectordb-shard
spec:
  clusterIP: None
  selector:
    app: vectordb-shard
  ports:
    - port: 50051
      targetPort: 50051
      name: grpc
